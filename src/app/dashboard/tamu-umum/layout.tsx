import { DashboardLayout } from '@/components/layout/DashboardLayout';

export default function TamuUmumLayout({ children }: { children: React.ReactNode }) {
  return <DashboardLayout title="Tamu Umum">{children}</DashboardLayout>;
}
